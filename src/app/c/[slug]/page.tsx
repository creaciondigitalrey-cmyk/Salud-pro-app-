"use client";
import { useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { motion } from "framer-motion";
import { AmbientBackground } from "@/components/saludpro/ambient-background";
import { Reveal, Eyebrow } from "@/components/saludpro/reveal";
import { BCVRateCard } from "@/components/saludpro/bcv-rate-card";
import { getProfession } from "@/lib/professions";
import { useBCV } from "@/lib/bcv-context";
import { Professional } from "@/lib/types";
import { MapPin, Clock, Shield, MessageCircle, Star, ArrowRight } from "lucide-react";
import QRCode from "qrcode";
export default function ProfessionalPage() {
  const params = useParams<{ slug: string }>();
  const [professional, setProfessional] = useState<Professional | null>(null);
  const [loading, setLoading] = useState(true);
  const [notFound, setNotFound] = useState(false);
  const [qrUrl, setQrUrl] = useState("");
  const { convert } = useBCV();
  useEffect(() => { const fetchP = async () => { try { const res = await fetch(`/api/professional?slug=${params.slug}`, { cache: "no-store" }); if (!res.ok) { setNotFound(true); setLoading(false); return; } const d = await res.json(); if (d.ok && d.professional) setProfessional(d.professional); else setNotFound(true); } catch { setNotFound(true); } finally { setLoading(false); } }; fetchP(); }, [params.slug]);
  useEffect(() => { if (typeof window === "undefined" || !professional) return; QRCode.toDataURL(`${window.location.origin}/c/${professional.slug}`, { errorCorrectionLevel: "H", margin: 2, width: 512, color: { dark: professional.colorAccent || "#0D9488", light: "#FFFFFF00" } }).then(setQrUrl).catch(() => {}); }, [professional]);
  if (loading) return <div className="min-h-screen flex items-center justify-center"><div className="w-16 h-16 rounded-full border-4 border-sp-teal/20 border-t-sp-teal animate-spin" /></div>;
  if (notFound || !professional) return <div className="min-h-screen flex items-center justify-center p-6"><div className="text-center"><h1 className="font-display text-4xl font-bold text-white mb-3">404</h1><p className="text-sp-text2 mb-6">No encontrado.</p><a href="/" className="text-sp-teal-light"><ArrowRight size={16} className="rotate-180 inline" /> Volver</a></div></div>;
  const config = getProfession(professional.profesion); const accent = professional.colorAccent || config.accent;
  const whatsappUrl = `https://wa.me/${professional.whatsapp.replace(/[^0-9]/g,"")}?text=${encodeURIComponent(`Hola ${professional.nombre}, vi tu portafolio en SaludPro`)}`;
  const initials = professional.nombre.split(" ").map(w=>w[0]).join("").slice(0,2).toUpperCase();
  return (
    <main className="relative">
      <AmbientBackground />
      <section className="relative min-h-[80vh] flex items-center px-6 py-16">
        <div className="max-w-6xl mx-auto w-full"><div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <div><Eyebrow color="teal">{config.label}</Eyebrow><h1 className="font-display text-5xl sm:text-6xl font-bold mt-6 mb-4">{professional.nombre}</h1><p className="text-xl text-sp-text2 mb-6">{professional.especialidad}</p><div className="flex flex-wrap gap-4 mb-8 text-sm text-sp-text2">{professional.ciudad && <span className="flex items-center gap-2"><MapPin size={16} style={{ color: accent }} />{professional.ciudad}</span>}<span className="flex items-center gap-2"><Clock size={16} style={{ color: accent }} />{professional.horarioTexto}</span>{professional.verificado && <span className="flex items-center gap-2 text-sp-health-green"><Shield size={16} />Verificado</span>}</div><div className="flex flex-col sm:flex-row gap-3"><a href={whatsappUrl} target="_blank" rel="noopener noreferrer" className="px-6 py-3 rounded-full font-semibold text-white flex items-center justify-center gap-2" style={{ background: "linear-gradient(135deg, #10B981, #059669)" }}><MessageCircle size={18} />WhatsApp</a></div><div className="mt-6"><BCVRateCard accent={accent} /></div></div>
          <div className="flex justify-center"><motion.div whileHover={{ scale: 1.02 }} className="relative w-64 h-64 sm:w-80 sm:h-80 rounded-3xl overflow-hidden" style={{ background: `linear-gradient(135deg, ${accent}30, ${accent}10)`, border: `2px solid ${accent}40`, boxShadow: `0 0 60px ${accent}40` }}>{professional.fotoUrl ? <img src={professional.fotoUrl} alt={professional.nombre} className="w-full h-full object-cover" /> : <div className="w-full h-full flex items-center justify-center"><span className="font-display text-8xl font-bold" style={{ color: accent }}>{initials}</span></div>}</motion.div></div>
        </div></div>
      </section>
      <section className="relative py-20 px-6"><div className="max-w-4xl mx-auto"><Reveal><Eyebrow color="cyan">Acerca de</Eyebrow></Reveal><Reveal delay={0.1}><h2 className="font-display text-3xl font-bold mt-6 mb-6">Conoce a {professional.nombre.split(" ")[0]}</h2></Reveal><Reveal delay={0.2}><p className="text-sp-text2 text-lg leading-relaxed">{professional.bio}</p></Reveal></div></section>
      <section id="servicios" className="relative py-20 px-6"><div className="max-w-6xl mx-auto"><div className="text-center mb-12"><Reveal><Eyebrow color="teal">Catálogo</Eyebrow></Reveal><Reveal delay={0.1}><h2 className="font-display text-3xl font-bold mt-6 mb-4">Servicios y <span className="text-gradient-teal">precios</span></h2></Reveal></div><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">{professional.servicios.filter(s=>s.activo).map((s, i) => (<Reveal key={s.id} delay={i*0.05}><div className="p-6 rounded-2xl glass hover-lift h-full flex flex-col"><div className="flex items-start justify-between mb-3"><h3 className="font-display text-lg font-semibold text-white flex-1">{s.nombre}</h3><div className="flex flex-col items-end ml-2"><span className="px-3 py-1 rounded-full text-sm font-bold" style={{ background: `${accent}20`, color: accent }}>${s.precio} USD</span><span className="text-[10px] text-sp-text2/70 mt-1">≈ Bs. {convert(s.precio)}</span></div></div><p className="text-sp-text2 text-sm mb-4 flex-1">{s.descripcion}</p><div className="text-xs text-sp-text2/70 mb-4"><Clock size={12} className="inline" /> {s.duracion}</div><a href={`https://wa.me/${professional.whatsapp.replace(/[^0-9]/g,"")}?text=${encodeURIComponent(`Hola, me interesa: ${s.nombre} ($${s.precio})`)}`} target="_blank" rel="noopener noreferrer" className="flex items-center justify-center gap-2 py-2.5 rounded-xl text-sm font-medium text-white" style={{ background: `${accent}30`, border: `1px solid ${accent}50` }}><MessageCircle size={14} />Reservar</a></div></Reveal>))}</div></div></section>
      {professional.testimonios && professional.testimonios.length > 0 && (<section className="relative py-20 px-6"><div className="max-w-5xl mx-auto"><div className="text-center mb-10"><Reveal><Eyebrow color="cyan">Reseñas</Eyebrow></Reveal><Reveal delay={0.1}><h2 className="font-display text-3xl font-bold mt-6 mb-4">Lo que dicen sus <span className="text-gradient-teal">pacientes</span></h2></Reveal></div><div className="grid grid-cols-1 md:grid-cols-3 gap-4">{professional.testimonios.map((t, i) => (<Reveal key={t.id} delay={i*0.1}><div className="p-6 rounded-2xl glass h-full flex flex-col"><div className="flex gap-1 mb-3">{Array.from({length:5}).map((_,idx)=><Star key={idx} size={14} color={idx<t.rating?"#D4AF37":"#CBD5E1"} fill={idx<t.rating?"#D4AF37":"none"} />)}</div><p className="text-sp-text2 text-sm mb-4 flex-1">"{t.comentario}"</p><div className="flex items-center gap-2"><div className="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold text-white" style={{ background: `linear-gradient(135deg, ${accent}, ${config.accentLight})` }}>{t.nombre[0]}</div><p className="text-xs font-medium text-white">{t.nombre}</p></div></div></Reveal>))}</div></div></section>)}
      <section className="relative py-32 px-6"><div className="max-w-3xl mx-auto text-center"><Reveal><h2 className="font-display text-4xl sm:text-5xl font-bold mb-6">¿Necesitas mis <span className="text-gradient-teal">servicios</span>?</h2></Reveal><Reveal delay={0.1}><p className="text-lg text-sp-text2 mb-10">Contáctame ahora.</p></Reveal><Reveal delay={0.2}><a href={whatsappUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-3 px-10 py-5 rounded-full font-bold text-white" style={{ background: "linear-gradient(135deg, #10B981, #059669)", boxShadow: "0 0 60px rgba(16,185,129,0.5)" }}><MessageCircle size={20} />Contactar</a></Reveal></div></section>
      <footer className="border-t border-white/5 py-8 px-6 text-center"><p className="text-xs text-sp-text2/60">© 2026 {professional.nombre} · SaludPro</p><p className="text-[10px] text-sp-text2/40 mt-1">Powered by Reinaldo Morales · Soluciones Digitales & IA · 0422-550-7708</p></footer>
      <motion.a href={whatsappUrl} target="_blank" rel="noopener noreferrer" initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ delay: 1 }} whileHover={{ scale: 1.1 }} className="fixed bottom-6 right-6 z-50 w-14 h-14 rounded-full flex items-center justify-center" style={{ background: "linear-gradient(135deg, #10B981, #059669)", boxShadow: "0 8px 30px rgba(16,185,129,0.5)" }}><span className="absolute inset-0 rounded-full animate-ping" style={{ background: "rgba(16,185,129,0.4)" }} /><svg width="28" height="28" viewBox="0 0 24 24" fill="white" className="relative z-10"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.967-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.872.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" /></svg></motion.a>
    </main>
  );
}
